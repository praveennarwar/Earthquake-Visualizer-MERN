# Client (React + Vite) for Earthquake Visualizer

Run in dev mode:
```
cd client
npm install
npm run dev
```

Open http://localhost:5173

If you want the server to serve the built client:
```
cd client
npm run build
# then run server
cd ../server
npm start
```
